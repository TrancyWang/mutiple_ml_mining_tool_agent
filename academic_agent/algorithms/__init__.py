"""mutiple_ml_mining_tool_agent 项目的 Python 源码包。

注意：此文件使本目录成为普通包（非命名空间包），避免与仓库内其他项目
（如 video_text_mutiplemodal_agent/src_codes）的同名顶层包冲突，
确保 `from academic_agent.algorithms.preprocessing...` 等绝对导入始终解析到本项目。
"""
