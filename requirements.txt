# Academic Agent 统一依赖
#
# 本文件同时覆盖：源码运行、Windows/macOS 桌面构建和 Qwen-Agent。
# 用户和构建机都只需要安装这一份依赖；ES、Milvus Lite 等服务仍可按需启用。

# 构建工具
pyinstaller>=6.0

# 基础依赖
pandas>=2.0.0
numpy>=1.24.0

# 向量数据库
chromadb>=0.4.0
elasticsearch>=8.0.0       # ES 会话全文记忆（可选）
pymilvus[milvus_lite]>=2.4.0  # 本地 Milvus Lite 语义记忆
llama-index-core>=0.14.0
llama-index-vector-stores-milvus>=1.1.0
llama-index-vector-stores-elasticsearch>=0.6.0

# 文本挖掘
jieba>=0.42.1
scikit-learn>=1.3.0
keybert>=0.7.0
sentence-transformers>=2.2.0  # 源项目 BGE 文本向量和聚类
transformers>=4.35.0          # 源项目情感模型
torch>=2.0.0                  # 源项目模型推理
umap-learn>=0.5.0             # 源项目可选降维

# 可视化
wordcloud>=1.9.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 机器学习
xgboost>=2.0.0
lightgbm>=4.0.0

# 数据处理
openpyxl>=3.1.0  # Excel支持
pypdf>=4.0.0     # PDF文本提取
PyMuPDF>=1.24.0  # PDF页图、坐标叠框与内嵌图片提取（Agentic RAG）
python-docx>=1.1.0
python-pptx>=0.6.23
pyarrow>=15.0.0   # Parquet/Feather
odfpy>=1.4.1      # ODS

# Agent 运行时
qwen-agent>=0.0.34

# PySide6 对话客户端
PySide6>=6.7.0
